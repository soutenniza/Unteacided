<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout_start"
    android:layout_width="match_parent"
    android:layout_height="match_parent">


    <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/startRL"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin"
        android:paddingBottom="@dimen/activity_vertical_margin"
        tools:context=".startActivity">

        <TextView
            android:id="@+id/teaStartName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="You should drink"
            android:layout_centerHorizontal="true"
            android:textColor="@color/welcome_color"
            android:textSize="25dp"/>

        <it.gmariotti.cardslib.library.view.CardListView
            android:paddingTop="5dp"
            android:layout_below="@id/teaStartName"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/cardList"
            android:divider="@android:color/transparent"/>
    </RelativeLayout>
    <ListView android:id="@+id/navigationDrawerList"
        android:layout_width="160dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:choiceMode="singleChoice"
        android:divider="@color/welcome_color"
        android:dividerHeight="2dp"
        android:background="#fff"/>

</android.support.v4.widget.DrawerLayout>
